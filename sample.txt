git init //レジストリの確保
git add . //ステージングエリアへ保存
git commit -m "(なんでもOK)"　//レポジトリへ保存
git log //ログを見る
git reset --head (ID)or(head^) //前に戻る
git checkout -b （ファイル名）　//ブランチの作成
git branch //ブランチの確認
git remote add origin https://github.com/soturon/sample.git　//宛先の登録
git push -u origin master //githubへ保存
